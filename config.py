bot_settings = dict(
    token = 'BotTokenHere',
    prefix = '!',
    purge_amount = 100
)

user_settings = dict(
    allowed_role = role_id_here,
    admin_allowed = True
)

message_settings = dict(
    suggestions_channel = "meals",
    finalize_channel_id = channel_id_here
)

reaction_settings = dict(
    emojis = ['👍', '👎']
)
